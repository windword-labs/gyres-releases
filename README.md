# gyres-releases

Public repo: https://github.com/windword-labs/gyres-releases

Prefer editing workflows in the public repo. This copy is the private-repo mirror of the intended layout.

Secrets:

- Public repo `PRIVATE_REPO_PAT` (read `windword-labs/jetstream`)
- Private repo `PUBLIC_RELEASE_REPO_PAT` (dispatch to gyres-releases)
- Public repo macOS signing: `CSC_LINK_BASE64`, `CSC_KEY_PASSWORD`, `APPLE_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, `APPLE_TEAM_ID`
